name: Deploy Infrastructure and Application

on:
  # Disabled auto-trigger - use manual trigger only
  # push:
  #   branches: [main]
  workflow_dispatch:  # Manual trigger only

env:
  AZURE_FUNCTIONAPP_PACKAGE_PATH: 'app/backend'
  PYTHON_VERSION: '3.11'
  NODE_VERSION: '18'
  TERRAFORM_VERSION: '1.5.0'

jobs:
  # ============================================
  # Job 1: Deploy Infrastructure with Terraform
  # ============================================
  infrastructure:
    name: 'Terraform Infrastructure'
    runs-on: ubuntu-latest
    outputs:
      function_app_name: ${{ steps.terraform-output.outputs.function_app_name }}
      function_app_url: ${{ steps.terraform-output.outputs.function_app_url }}
      static_web_app_name: ${{ steps.terraform-output.outputs.static_web_app_name }}
      static_web_app_url: ${{ steps.terraform-output.outputs.static_web_app_url }}
      resource_group: ${{ steps.terraform-output.outputs.resource_group }}
    
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Terraform
        uses: hashicorp/setup-terraform@v3
        with:
          terraform_version: ${{ env.TERRAFORM_VERSION }}
          terraform_wrapper: false

      - name: Azure Login
        uses: azure/login@v1
        with:
          creds: ${{ secrets.AZURE_CREDENTIALS }}

      - name: Terraform Init
        working-directory: .
        run: |
          terraform init
          echo "✅ Terraform initialized"

      - name: Terraform Validate
        working-directory: .
        run: |
          terraform validate
          echo "✅ Terraform configuration validated"

      - name: Terraform Format Check
        working-directory: .
        run: |
          terraform fmt -recursive -check || true
          echo "✅ Terraform format checked"

      - name: Check if resources exist
        id: check-resources
        run: |
          # Try to get the function app - if it exists, skip terraform apply
          RESOURCE_GROUP=$(cat dev.tfvars | grep 'resource_group_name' | cut -d'=' -f2 | tr -d ' "')
          FUNCTION_APP=$(cat dev.tfvars | grep 'function_app_name' | cut -d'=' -f2 | tr -d ' "')
          
          if az functionapp show --resource-group "$RESOURCE_GROUP" --name "$FUNCTION_APP" &>/dev/null; then
            echo "exists=true" >> $GITHUB_OUTPUT
            echo "✅ Resources already exist - skipping infrastructure deployment"
          else
            echo "exists=false" >> $GITHUB_OUTPUT
            echo "ℹ️ Resources not found - will deploy infrastructure"
          fi

      - name: Terraform Plan
        if: steps.check-resources.outputs.exists == 'false'
        working-directory: .
        run: |
          terraform plan -var-file="dev.tfvars" -out=tfplan
          echo "✅ Terraform plan created"

      - name: Terraform Apply
        if: steps.check-resources.outputs.exists == 'false'
        working-directory: .
        run: |
          terraform apply -auto-approve tfplan
          echo "✅ Infrastructure deployed"
        
      - name: Initialize Terraform for existing resources
        if: steps.check-resources.outputs.exists == 'true'
        working-directory: .
        run: |
          terraform init
          echo "✅ Terraform initialized for existing resources"

      - name: Get Terraform Outputs
        id: terraform-output
        working-directory: .
        run: |
          # Refresh state if resources already exist
          terraform refresh -var-file="dev.tfvars" || true
          
          echo "function_app_name=$(terraform output -raw function_app_name)" >> $GITHUB_OUTPUT
          echo "function_app_url=$(terraform output -raw function_app_url)" >> $GITHUB_OUTPUT
          echo "static_web_app_name=$(terraform output -raw static_web_app_name)" >> $GITHUB_OUTPUT
          echo "resource_group=$(terraform output -raw resource_group_name)" >> $GITHUB_OUTPUT
          echo "✅ Terraform outputs extracted"

  # ============================================
  # Job 2: Deploy Function App (Backend)
  # ============================================
  deploy-backend:
    name: 'Deploy Function App (Python Backend)'
    needs: infrastructure
    runs-on: ubuntu-latest
    
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Python
        uses: actions/setup-python@v4
        with:
          python-version: ${{ env.PYTHON_VERSION }}

      - name: Install Python dependencies
        working-directory: ${{ env.AZURE_FUNCTIONAPP_PACKAGE_PATH }}
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt --target=".python_packages/lib/site-packages"
          echo "✅ Python dependencies installed"

      - name: Azure CLI Login
        uses: azure/login@v1
        with:
          creds: ${{ secrets.AZURE_CREDENTIALS }}

      - name: Deploy Function App Code
        uses: Azure/functions-action@v1
        with:
          app-name: ${{ needs.infrastructure.outputs.function_app_name }}
          package: ${{ env.AZURE_FUNCTIONAPP_PACKAGE_PATH }}
          scm-do-build-during-deployment: true
          enable-oryx-build: true

      - name: Verify Function App Deployment
        run: |
          # Wait for function app to be ready
          sleep 10
          
          # Test health endpoint
          HEALTH_CHECK=$(curl -s -o /dev/null -w "%{http_code}" "${{ needs.infrastructure.outputs.function_app_url }}/api/health")
          
          if [ "$HEALTH_CHECK" = "200" ]; then
            echo "✅ Function App is healthy"
          else
            echo "⚠️ Function App health check returned: $HEALTH_CHECK"
          fi

  # ============================================
  # Job 3: Deploy Static Web App (Frontend)
  # ============================================
  deploy-frontend:
    name: 'Deploy Static Web App (React Frontend)'
    needs: [infrastructure, deploy-backend]
    runs-on: ubuntu-latest
    
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: ${{ env.NODE_VERSION }}
          cache: 'npm'
          cache-dependency-path: app/frontend/package-lock.json

      - name: Install Node dependencies
        working-directory: app/frontend
        run: |
          npm ci
          echo "✅ Node dependencies installed"

      - name: Build React Frontend
        working-directory: app/frontend
        env:
          REACT_APP_API_URL: ${{ needs.infrastructure.outputs.function_app_url }}/api
          CI: false
        run: |
          npm run build
          echo "✅ Frontend built successfully"

      - name: Deploy to Static Web App
        uses: Azure/static-web-apps-deploy@v1
        with:
          azure_static_web_apps_api_token: ${{ secrets.AZURE_STATIC_WEB_APPS_API_TOKEN }}
          repo_token: ${{ secrets.GITHUB_TOKEN }}
          action: "upload"
          app_location: "app/frontend/build"
          skip_app_build: true

  # ============================================
  # Job 4: Verify Deployment & Output Results
  # ============================================
  verify-deployment:
    name: 'Verify Deployment & Output Results'
    needs: [infrastructure, deploy-backend, deploy-frontend]
    runs-on: ubuntu-latest
    
    steps:
      - name: Test API Endpoints
        run: |
          echo "🧪 Testing API endpoints..."
          
          # Health check
          HEALTH=$(curl -s -w "\n%{http_code}" "${{ needs.infrastructure.outputs.function_app_url }}/api/health")
          HTTP_CODE=$(echo "$HEALTH" | tail -n1)
          BODY=$(echo "$HEALTH" | head -n1)
          
          echo "Health Check Response: $BODY"
          
          if [ "$HTTP_CODE" = "200" ]; then
            echo "✅ Health endpoint working"
          else
            echo "⚠️ Health endpoint returned: $HTTP_CODE"
          fi

      - name: Output Deployment URLs
        run: |
          echo ""
          echo "╔═════════════════════════════════════════════════════════════╗"
          echo "║           🎉 DEPLOYMENT COMPLETED SUCCESSFULLY 🎉           ║"
          echo "╚═════════════════════════════════════════════════════════════╝"
          echo ""
          echo "📋 Deployment Summary:"
          echo "────────────────────────────────────────────────────────────"
          echo ""
          echo "🌐 FRONTEND (React Application):"
          echo "   Static Web App Name: ${{ needs.infrastructure.outputs.static_web_app_name }}"
          echo ""
          echo "⚙️  BACKEND (Python Function App):"
          echo "   Function App Name: ${{ needs.infrastructure.outputs.function_app_name }}"
          echo "   Function App URL:  ${{ needs.infrastructure.outputs.function_app_url }}"
          echo "   Health Endpoint:   ${{ needs.infrastructure.outputs.function_app_url }}/api/health"
          echo ""
          echo "📊 ENDPOINTS:"
          echo "   Get Employees:     GET ${{ needs.infrastructure.outputs.function_app_url }}/api/employees"
          echo "   Create Employee:   POST ${{ needs.infrastructure.outputs.function_app_url }}/api/employees"
          echo "   Update Employee:   PUT ${{ needs.infrastructure.outputs.function_app_url }}/api/employees/{id}"
          echo "   Delete Employee:   DELETE ${{ needs.infrastructure.outputs.function_app_url }}/api/employees/{id}"
          echo ""
          echo "📚 RESOURCES:"
          echo "   Resource Group: ${{ needs.infrastructure.outputs.resource_group }}"
          echo "   Region: eastus (configurable in dev.tfvars)"
          echo ""
          echo "🔐 SECURITY:"
          echo "   ✅ System-Assigned Managed Identity enabled"
          echo "   ✅ RBAC roles configured (Cosmos DB, Key Vault, Storage)"
          echo "   ✅ VNet Integration enabled"
          echo "   ✅ Private Endpoints configured"
          echo "   ✅ HTTPS-only enforcement"
          echo ""
          echo "📝 NEXT STEPS:"
          echo "   1. Wait 2-3 minutes for Static Web App to fully provision"
          echo "   2. Visit the Static Web App URL to access the frontend"
          echo "   3. Test the Employee Management APIs"
          echo "   4. Check Azure Portal for resource details"
          echo ""
          echo "────────────────────────────────────────────────────────────"
          echo "Deploy Time: $(date -u +'%Y-%m-%dT%H:%M:%SZ')"
          echo "────────────────────────────────────────────────────────────"
